```markdown
# PROPOSAL-2025-0099: Обновление контекста проекта Trader

## Диагностика

Текущая документация `trader/docs` хорошо описывает роль, прошлые спринты и backlog, но корневая папка `trader/` не содержит краткого `README.md` с быстрым обзором проекта и указанием важнейших точек входа. Это затрудняет быстрый обзор проекта новым участникам и внешним интеграторам.

## Предлагаемое изменение

1. Добавить `trader/README.md` с кратким обзором задачи, архитектурой, зависимостями и основными точками входа (проекты в solution: Host, Core, Exchanges).
2. Добавить в `trader/docs/backlog.md` секцию "Контекст" с ссылкой на `README.md` и наиболее важными задачами.

Ниже — предлагаемый diff для `README.md` (предпросмотр). После подтверждения — реализовать изменения.

```diff
*** Add File: trader/README.md
+ # Trader — краткий обзор

+ Проект `Trader` реализует арбитражный торговый бот с поддержкой двух ног (Gate.io ↔ Bybit).
+
+ Основные компоненты:
+
+ - `src/Core` — бизнес-логика и контроль арбитража
+ - `src/Exchanges` — адаптеры для бирж, включая Bybit и Gate.io
+ - `src/Host` — точка входа/виконання службы
+
+ Текущий статус: Leg 1 и Leg 2 реализованы; идёт работа по рефакторингу и стабилизации.
```

## Обоснование

 - `README.md` улучшит доступность проекта для новых участников и команд, которые интегрируются с репозиторием.
 - Явное описание точек входа ускорит понимание архитектуры и поможет при отладке и CI.

## Оценка рисков

 - Изменения документальные — низкий риск.
 - Возможные конфликты: изменения блокнотов/документации, не влияющие на кодовую базу.

## План тестирования

 - Проверить корректность рендеринга `README.md` локально: открыть файл в VS Code / GitHub.
 - Обновить ссылки в документации и убедиться, что не осталось битых ссылок.

## План отката

 - Удалить добавленный `README.md` и отменить секцию в `backlog.md`.

---
```
