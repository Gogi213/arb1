# АУДИТ COLLECTIONS - КРИТИЧЕСКИЕ ПРОБЛЕМЫ EVENT-DRIVEN АРХИТЕКТУРЫ

**Дата аудита:** 2025-11-09
**Аудитор:** Claude Code
**Статус:** ✅ ЗАВЕРШЕН - КРИТИЧЕСКИЕ ПРОБЛЕМЫ ОБНАРУЖЕНЫ

---

## 🎯 ЦЕЛЬ АУДИТА

Провести анализ collections после внедрения event-driven архитектуры. Выяснить причины:
- Множественных точек на одном timestamp на графиках
- Периодических обновлений вместо истинного event-driven
- Проблем с bid/bid логики

---

## 🔍 РЕЗУЛЬТАТЫ АНАЛИЗА

### 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА #1: Event-Driven Архитектура Сломана

**Описание:** При каждом событии отправляется ВСЯ история 30-минутного окна вместо новых данных.

**Root Cause:**
```csharp
// RollingWindowService.JoinRealtimeWindows()
var joined = AsOfJoin(data1, data2, TimeSpan.FromSeconds(2));
// Возвращает ВЕСЬ массив joined точек (сотни/тысячи точек)
return new RealtimeChartData {
    Timestamps = epochTimestamps,  // ВСЯ ИСТОРИЯ!
    Spreads = spreads,            // ВСЯ ИСТОРИЯ!
    // ...
};
```

**Последствия:**
- ❌ Клиент получает тысячи точек вместо 1 новой
- ❌ uPlot накладывает точки с одинаковыми timestamps
- ❌ Графики выглядят как "несколько точек на одном timestamp"
- ❌ Обновления кажутся периодическими из-за перегрузки

### 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА #2: AsOfJoin Создает Дубликаты Timestamps

**Описание:** Tolerance 2 секунды позволяет одной точке join'иться с несколькими близкими.

**Код проблемы:**
```csharp
private List<(DateTime timestamp, decimal bid1, decimal bid2)> AsOfJoin(
    List<(DateTime ts, decimal bid)> data1,
    List<(DateTime ts, decimal bid)> data2,
    TimeSpan tolerance)  // tolerance = 2 секунды
{
    // Одна точка может join'иться с несколькими в окне 2с
    // Результат: множественные точки с одинаковыми timestamps
}
```

**Результат:** Множественные точки с одинаковыми timestamps в результате join.

### 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА #3: BidBidLogger Логирует Полную Историю

**Описание:** При каждом вызове логируется ВСЯ история joined точек.

**Код проблемы:**
```csharp
// JoinRealtimeWindows() логирует ВСЕ точки
if (_bidBidLogger != null) {
    _ = Task.Run(async () => {
        for (int i = 0; i < joined.Count; i++) {  // joined.Count может быть 1000+
            await _bidBidLogger.LogAsync(...);   // Логирует всю историю!
        }
    });
}
```

---

## ✅ ЧТО РАБОТАЕТ ПРАВИЛЬНО

### Event-Driven Инфраструктура
- ✅ События поднимаются в `RollingWindowService.ProcessData()`
- ✅ `RealTimeController` правильно подписывается на события
- ✅ Thread-safe отправка через `SemaphoreSlim(1,1)`
- ✅ Автоматическая отписка в `finally` блоке

### BidBidLogger Архитектура
- ✅ Channel-based для non-blocking логирования
- ✅ Только ICPUSDT фильтрация
- ✅ Bounded channel с `DropOldest`
- ✅ Background task для записи

### WebSocket Коммуникация
- ✅ Правильный формат JSON сообщений
- ✅ Graceful close handling
- ✅ Error handling с logging

---

## 🔧 НЕОБХОДИМЫЕ ИСПРАВЛЕНИЯ

### Исправление #1: Изменить JoinRealtimeWindows (КРИТИЧНО)

**Текущий код (ПЛОХОЙ):**
```csharp
public RealtimeChartData? JoinRealtimeWindows(string symbol, string exchange1, string exchange2)
{
    // ... AsOfJoin возвращает всю историю ...
    var joined = AsOfJoin(data1, data2, TimeSpan.FromSeconds(2));

    // Возвращает ВСЮ ИСТОРИЮ
    return new RealtimeChartData {
        Timestamps = joined.Select(x => ...).ToList(),  // Тысячи точек!
        Spreads = joined.Select(x => ...).ToList(),     // Тысячи точек!
        // ...
    };
}
```

**Исправленный код (ХОРОШИЙ):**
```csharp
public RealtimeChartData? JoinRealtimeWindows(string symbol, string exchange1, string exchange2)
{
    // ... AsOfJoin ...
    var joined = AsOfJoin(data1, data2, TimeSpan.FromSeconds(2));

    // Возвращает ТОЛЬКО ПОСЛЕДНЮЮ ТОЧКУ
    var lastPoint = joined.LastOrDefault();
    if (lastPoint == default) return null;

    var spread = CalculateSpread(lastPoint.bid1, lastPoint.bid2);
    var upperBand = CalculateQuantile(joined.Select(x => CalculateSpread(x.bid1, x.bid2)), 0.97);
    var lowerBand = CalculateQuantile(joined.Select(x => CalculateSpread(x.bid1, x.bid2)), 0.03);

    return new RealtimeChartData {
        Timestamps = new List<double> { ((DateTimeOffset)lastPoint.timestamp).ToUnixTimeSeconds() },
        Spreads = new List<double?> { spread },
        UpperBand = new List<double?> { upperBand },
        LowerBand = new List<double?> { lowerBand }
    };
}
```

### Исправление #2: Оптимизировать AsOfJoin Tolerance

**Рекомендация:** Уменьшить tolerance до 500ms для уменьшения дубликатов.

```csharp
var joined = AsOfJoin(data1, data2, TimeSpan.FromMilliseconds(500));
```

### Исправление #3: Исправить BidBidLogger

**Текущий код логирует всю историю:**
```csharp
for (int i = 0; i < joined.Count; i++) {  // Логирует 1000+ точек
    await _bidBidLogger.LogAsync(joined[i]);
}
```

**Исправленный код логирует только новые точки:**
```csharp
// Логировать только последнюю точку
var lastPoint = joined.LastOrDefault();
if (lastPoint != default) {
    await _bidBidLogger.LogAsync(lastPoint);
}
```

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ ПОСЛЕ ИСПРАВЛЕНИЙ

### До исправлений:
```
Графики: [Точка1, Точка1, Точка1, Точка2, Точка2, ...] (дубликаты)
Обновления: Каждые 500ms → burst из N сообщений
Производительность: Низкая (тысячи точек в сообщении)
```

### После исправлений:
```
Графики: [Точка1, Точка2, Точка3, ...] (уникальные timestamps)
Обновления: Только при новых данных (event-driven)
Производительность: Высокая (1 точка в сообщении)
```

---

## 🎯 ПРИОРИТЕТЫ ИСПРАВЛЕНИЙ

### 🔥 КРИТИЧНЫЙ (СРОЧНО)
1. **Изменить JoinRealtimeWindows()** - возвращать только последнюю точку
2. **Тестирование** - проверить что графики обновляются корректно

### ⚠️ ВЫСОКИЙ ПРИОРИТЕТ
3. **Оптимизировать AsOfJoin tolerance** - уменьшить до 500ms
4. **Исправить BidBidLogger** - логировать только новые точки

### 📈 СРЕДНИЙ ПРИОРИТЕТ
5. **Добавить метрики** - логировать количество отправленных точек
6. **Профилирование** - измерить latency event → WebSocket

---

## 📋 ТЕСТИРОВАНИЕ ПОСЛЕ ИСПРАВЛЕНИЙ

### Тест #1: Event-Driven Проверка
```bash
# Запустить collections
dotnet run --project src/SpreadAggregator.Presentation

# В браузере открыть dashboard
# Наблюдать: обновления только при реальных изменениях данных
# Ожидание: нет множественных точек на одном timestamp
```

### Тест #2: WebSocket Сообщения
```javascript
// В браузере console
webSocket.onmessage = (event) => {
    const data = JSON.parse(event.data);
    console.log('Timestamps count:', data.timestamps.length);  // Должно быть 1
    console.log('Sample timestamp:', data.timestamps[0]);
};
```

### Тест #3: BidBid Логи
```bash
# Проверить логи
tail -f collections/logs/bidbid_ICPUSDT_*.log
# Ожидание: только новые точки, не вся история
```

---

## 📚 ДОКУМЕНТАЦИЯ

**Связанные файлы:**
- `аудит/REALTIME_BATCHING_AUDIT.md` - анализ event-driven
- `аудит/CHANGELOG.md` - история изменений
- `docs/collections_context.md` - контекст программы

**Ключевые файлы для исправления:**
- `collections/src/SpreadAggregator.Application/Services/RollingWindowService.cs`
- `collections/src/SpreadAggregator.Presentation/Controllers/RealTimeController.cs`
- `collections/src/SpreadAggregator.Infrastructure/Services/BidBidLogger.cs`

---

## ✅ ВЫВОДЫ

**Основная проблема:** Event-driven архитектура возвращает полную историю вместо дельты изменений.

**Решение:** Изменить `JoinRealtimeWindows()` на возврат только последней точки join.

**Ожидаемый эффект:** Истинная event-driven работа, уникальные timestamps, высокая производительность.

---

**Аудит завершен:** 2025-11-09
**Рекомендация:** Применить исправления в порядке приоритета
